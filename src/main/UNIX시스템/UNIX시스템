시 분할 운영체제

MBR

디바이스

데몬

메타데이터

NPM

마운트
 장비를 파일 시스템 트리에 부착하여 운영체제가 장비를 인식할 수 있게 하는것

HEAD
 현재의 작업 브랜치를 가리킴

명령어들
ls [option][names] 파일과 디렉터리를 나열하는 명령. 사용빈도 높음
    -a  --all           모든 파일 보여줌. 점(.)으로 시작하는 숨김파일도 보여줌
    -d  --directory     디렉터리 자체에 대한 정보 보여줌
    -F  --classify      우측에 파일의 종류를 알려주는 문자를 붙임 실행파일은* 디렉터리는/
    -l  --format=long   긴 포맷으로 결과를 보여줌
    -R  --recursive     재귀적으로 수행되는데 서브디렉터리의 내용도 나열
    -S  --sort=size     파일의 크기 순서로 결과를 보여줌
    -t  --sort=time     최종 수정 시간 순으로 보여줌

mkdir [options] directories 디렉터리를 만드는 명령
git
   config
   init
   status
   add
   log
   checkout
   diff
   switch
   branch
   stash
   reset
   revert